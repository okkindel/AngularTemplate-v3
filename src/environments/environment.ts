export const environment = {
  app_url: '/',
  api: 'http://localhost:8080/api',
  production: true,
  build: 'prod',
};

export const APP_URL = environment.app_url;
export const API_URL = environment.api;
